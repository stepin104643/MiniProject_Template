#include <stdio.h>
#include <stdlib.h>
#include<string.h>

int main()
{
    char string[]= "my_name_is_edcast_future_skills";

    char *token=strtok(string,"_");

    while(token!=NULL){
        printf("%s ",token);
        token=strtok(NULL,"_");
    }
}
